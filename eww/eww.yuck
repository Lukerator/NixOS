(deflisten notifs :initial ""
  `python ~/.config/nixos/eww/main.py`)

(defwindow powermenu
	:monitor 0
	:stacking "overlay"
	:geometry (geometry
		:width "20%"
		:height "20%"
		:anchor "top right"
	)
	(literal :content notifs)
)
